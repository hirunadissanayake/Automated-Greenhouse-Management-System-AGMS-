spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:${user.dir}/config-repo,file:${user.dir}/../config-repo,file:${user.dir}/../../config-repo

server:
  port: 8888

server:
  port: 8081

agms:
  external:
    iot:
      base-url: ${AGMS_IOT_BASE_URL:http://localhost:8080/api}
      username: ${AGMS_IOT_USERNAME:agms_user}
      password: ${AGMS_IOT_PASSWORD:123456}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
